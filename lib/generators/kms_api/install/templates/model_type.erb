Types::Kms::ModelType = GraphQL::ObjectType.define do
  name "KmsModel"
  # Add root-level fields here.
  # They will be entry points for queries on your schema.

  field :id, types.Int
  field :kms_model_name, types.String
  field :collection_name, types.String
end
